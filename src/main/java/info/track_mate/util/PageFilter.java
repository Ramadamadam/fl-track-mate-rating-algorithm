package info.track_mate.util;

public class PageFilter {
    public PageFilter(String s, ParameterOperator equals, Long horseId, FilterOperator and) {
    }

    public enum ParameterOperator {
        EQUALS
    }

    public enum FilterOperator {
        AND

    }
}
